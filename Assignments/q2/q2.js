//Name Cases: Store a person’s name in a variable,
// and then print that person’s name in lowercase, uppercase, and titlecase.
var username = "Anas"; //this saves the name
console.log(username.toLowerCase()); // shows the name in all small letters
console.log(username.toUpperCase()); // shows the name in all bid letters
console.log(username.charAt(0).toUpperCase() + username.slice(1).toLowerCase()); //show the name with first letter big
